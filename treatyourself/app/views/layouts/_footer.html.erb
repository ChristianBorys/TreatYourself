<div class="footer">
	<p>Enterprise</p>
	<% if current_owner %>
		Signed in as <%= current_owner.name.capitalize %>, <%= link_to "Log out", session_path("current"), :method => :delete %>
	<% else %>
		<%= link_to "Log In", new_session_path %> or <%= link_to "Sign Up", new_owner_path %>
	<% end %>
</div> 